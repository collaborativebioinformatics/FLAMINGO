# Introduction

Federated learning approaches address a series of data privacy issues where problems that require/benefit from data from multiple databases are necessary but cannot be shared between databases. 